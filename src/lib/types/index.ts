export type BookDemoType = {
  firstName: string;
  lastName?: string;
  companyName: string;
  email: string;
  phone: string;
  employeeCount: string;
  details: string;
};

export type WaitlistType = {
  firstName: string;
  lastName?: string;
  email: string;
};

export type ContactType = {
  firstName: string;
  lastName?: string;
  companyName: string;
  email: string;
  details: string;
};
